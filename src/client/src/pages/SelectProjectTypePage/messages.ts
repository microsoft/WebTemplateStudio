import { defineMessages } from "react-intl";

const messages = defineMessages({
  header: {
    id: "selectProjectTypePage.header",
    defaultMessage: "Select the type of application",
  },
});
export default messages;
