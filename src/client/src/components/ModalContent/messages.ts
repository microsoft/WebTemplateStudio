import { defineMessages } from "react-intl";

const messages = defineMessages({
  ariaCloseModalLabel: {
    id: "ModalContent.ariaCloseModalLabel",
    defaultMessage: "Close Modal",
  },
});

export default messages;
