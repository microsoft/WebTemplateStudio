import { defineMessages } from "react-intl";

const messages = defineMessages({
  yourProjectDetails: {
    id: "rightSidebar.reactNative.yourProjectDetails",
    defaultMessage: "Your React Native App",
  },
});

export default messages;
