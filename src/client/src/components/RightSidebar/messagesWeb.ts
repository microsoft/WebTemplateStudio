import { defineMessages } from "react-intl";

const messages = defineMessages({
  yourProjectDetails: {
    id: "rightSidebar.web.yourProjectDetails",
    defaultMessage: "Your Web App",
  },
});

export default messages;
