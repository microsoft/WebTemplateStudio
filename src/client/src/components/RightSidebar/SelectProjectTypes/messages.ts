import { defineMessages } from "react-intl";

const messages = defineMessages({
  selectProjectTypes: {
    id: "rightSidebar.selectProjectTypes",
    defaultMessage: "Project Type",
  },
});

export default messages;
